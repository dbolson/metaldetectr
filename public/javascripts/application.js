$(function() {
  $('#lastfm_sync').submit(function() {
    /*
    var $form = $(this);
    $.post(
      $form.attr('action'),
      $form.serialize(),
      function(data) {
        $form.after(data);
      }
    );
    */

    return false;
  });
});
